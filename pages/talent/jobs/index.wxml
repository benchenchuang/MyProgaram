<!--pages/talent/jobs/index.wxml-->
<view wx:if="{{jobs.length}}">
  <job-item jobs="{{jobs}}"/>
</view>
<view wx:else>
  <view wx:if="{{isLoad}}" class="no_data">获取列表中</view>
  <view wx:else class="no_data">暂无数据</view>
</view>
